var numArr = [6,10,2,3,5,];

//var sortedArr = numArr.sort();

//console.log(sortedArr);

valueSort(numArr);

function valueSort(array) {
    var num = array[0];
    
    for(let i=0; i < array.length; i++ ){
        console.log( array[i] );
    }
}